const NotFoundPage = () => {
  return (
    <div
      style={{
        width: "100%",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        minHeight: "400px",
      }}
    >
      <h1>페이지를 찾을 수 없습니다!</h1>
    </div>
  );
};

export default NotFoundPage;
